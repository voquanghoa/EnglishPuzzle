<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.qhvv.englishpuzzle" >
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:hardwareAccelerated="true"
        android:theme="@style/AppTheme"
        android:name=".application.EnglishPuzzleApplication"
        >
        <activity
            android:name=".CategorySelectionActivity"
            android:label="@string/select_category" >

        </activity>
        <activity
            android:name=".DownloadPackageActivity"
            android:label="@string/app_name" >

        </activity>

        <activity
            android:name=".MainMenuActivity"
            android:label="@string/puzzle1"
            android:launchMode= "singleInstance"
            />
        <activity
            android:name=".ExerciseSelectPictureActivity"
            android:label="@string/puzzle1"
            android:launchMode= "singleInstance"
            />

        <activity
            android:name=".SplashActivity"
            android:label="@string/puzzle1"
            android:launchMode= "singleInstance"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainPuzzleActivity"
            android:launchMode= "singleInstance"
            >

        </activity>

    </application>

</manifest>
